/**
 * Integration 6: SISKOPATUH Submission Status
 */

export enum SubmissionStatus {
  PENDING = "pending",
  SUBMITTED = "submitted",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  FAILED = "failed",
}
