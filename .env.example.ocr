# ================================================================
# OCR INTEGRATION (VERIHUBS) - INTEGRATION 1
# ================================================================
# Indonesia-optimized OCR for KTP, Passport, and Kartu Keluarga
# Provider: Verihubs (https://verihubs.com)
# Cost: ~$450/month for 1,000 jamaah (3 documents each)
# ================================================================

# OCR ENABLED (STUB vs PRODUCTION MODE)
# Set to 'false' for STUB mode (returns mock data, no API calls)
# Set to 'true' for PRODUCTION mode (calls Verihubs API)
OCR_ENABLED=false

# VERIHUBS API CREDENTIALS
# Obtain from: https://dashboard.verihubs.com
# Sign up required for production use
VERIHUBS_API_URL=https://api.verihubs.com/v1
VERIHUBS_API_KEY=your_verihubs_api_key_here
VERIHUBS_API_SECRET=your_verihubs_api_secret_here

# OCR CONFIGURATION
# Auto-approve documents with confidence >= threshold (0-100)
# Recommended: 80 (documents below 80% require manual review)
OCR_AUTO_APPROVE_THRESHOLD=80

# Enable pre-processing quality checks (brightness, blur, resolution, orientation)
# Recommended: true (reduces API costs by rejecting poor quality images)
OCR_QUALITY_CHECK_ENABLED=true

# Cache TTL for OCR results (seconds)
# Recommended: 3600 (1 hour) - reduce repeated API calls
OCR_CACHE_TTL=3600

# ================================================================
# COST OPTIMIZATION NOTES
# ================================================================
# 1. Quality checks (OCR_QUALITY_CHECK_ENABLED=true) prevent
#    sending poor quality images to API, saving costs
# 2. Caching (OCR_CACHE_TTL=3600) prevents re-processing same document
# 3. STUB mode (OCR_ENABLED=false) for development/testing
# 4. Monitor usage via GET /api/v1/ocr/statistics endpoint
#
# Estimated costs (Verihubs pricing):
# - KTP OCR: IDR 1,500 per document
# - Passport OCR: IDR 2,000 per document
# - KK OCR: IDR 1,500 per document
#
# For 1,000 jamaah/month:
# - 1,000 KTP × IDR 1,500 = IDR 1,500,000
# - 1,000 Passport × IDR 2,000 = IDR 2,000,000
# - 1,000 KK × IDR 1,500 = IDR 1,500,000
# TOTAL: ~IDR 5,000,000/month (~$325 USD)
# ================================================================

# ================================================================
# DEVELOPMENT SETUP
# ================================================================
# 1. Keep OCR_ENABLED=false during development
# 2. Use STUB mode to test integration without API calls
# 3. Mock data includes realistic Indonesian document examples
# 4. Test quality validation with sample images
# 5. Monitor logs for "STUB mode" vs "PRODUCTION mode" messages
# ================================================================

# ================================================================
# PRODUCTION DEPLOYMENT
# ================================================================
# 1. Sign up for Verihubs account at https://verihubs.com
# 2. Obtain API credentials from dashboard
# 3. Add credentials to .env file (DO NOT commit to git)
# 4. Set OCR_ENABLED=true
# 5. Test with real documents in staging environment
# 6. Monitor costs via Verihubs dashboard
# 7. Set up alerts for high usage
# ================================================================
